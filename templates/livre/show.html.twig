{% extends 'base.html.twig' %}

{% block title %}Livre{% endblock %}

{% block body %}
    <h1>Livre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ livre.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ livre.titre }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ livre.auteur }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ livre.categorie }}</td>
            </tr>
            <tr>
                <th>ISBN</th>
                <td>{{ livre.ISBN }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ livre.Prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_livre_index') }}" class="btn btn-success">back to list</a>

    <a href="{{ path('app_livre_edit', {'id': livre.id}) }}" class="btn btn-success">edit</a>

    {{ include('livre/_delete_form.html.twig') }}
{% endblock %}
