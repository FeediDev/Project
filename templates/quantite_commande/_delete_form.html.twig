<form method="post" action="{{ path('app_quantite_commande_delete', {'id': quantite_commande.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quantite_commande.id) }}">
    <button class="btn">Delete</button>
</form>
